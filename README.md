# clean-my-mac

Safe disk cleanup analysis and tooling for macOS.

## Commands

- `npm run dev -- scan` → analyze default targets and write a report.
- `npm run dev -- ncdu ~/Library` → open `ncdu` for manual review.

## Install `ncdu`

`brew install ncdu`
